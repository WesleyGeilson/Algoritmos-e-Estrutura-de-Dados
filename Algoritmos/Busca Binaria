// -------------- ALGORITMO CAÇADA DO INTERVALO (BUSCA BINARIA) -------------- // achar elemento de maneira eficiente
// Vantagem: Eficiente e facil de entende, porem o vetor precisa estar previamente ordenado
// Dado o vetor ordenado(seja por bubble, merge, ou qualquer outra ordenação)

int i = 1
int j = n
ref = (i + j)/2
k

enquanto(i <= j){
    se(vet[ref] = k){
        imprimir("O valor que vc busca esta no meio")
        parar
    }
    se(vet[ref] < k){
        // seu valor esta inserido em todos os valores da direira, portanto podemos descartar os valores da esquerda
        i = ref+1
    }
    se (vet[ref] > k){
        // seu valor esta inserido em todos os valores da esquerda, portanto podemos descartar os valores da direita
        j = ref-1
    }
    ref = (i + j)/2
    
} // enquanto i for menor que j, nos garante que ele ira percorrer ate encontar um intervalo ideial e quando as dois indices forem iguais isso implica que encontramos o numero que queriamos


O(log(n))